providers:
  kubernetesGateway:
    enabled: true

gateway:
  listeners:
    web:
      namespacePolicy:
        from: All

ports:
  web:
    port: 80
    redirectTo: websecure
  websecure:
    port: 443
