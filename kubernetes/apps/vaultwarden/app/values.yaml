signupsAllowed: false

domain: "https://warden.salami.network"

resourceType: Deployment

adminToken:
  existingSecret: "vaultwarden-admin-token"
  existingSecretKey: "token"

database:
  type: postgresql
  existingSecret: "vaultwarden-db-app"
  existingSecretKey: "uri"
